//import liraries
import React from "react";
import { View, Text, SafeAreaView } from "react-native";

// create a component
const ProfileTab = () => {
	return (
		<SafeAreaView>
			<Text>Profile</Text>
		</SafeAreaView>
	);
};

export default ProfileTab;
